<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:TaskMaster.ViewModels"
             x:Class="TaskMaster.Views.TasksPage"
             Title="Mes Tâches">
    
    <Grid RowDefinitions="Auto, *">
        <!-- En-tête avec bouton d'ajout -->
        <HorizontalStackLayout Grid.Row="0" 
                             Margin="10"
                             Spacing="10">
            <Button Text="Nouvelle Tâche"
                    Clicked="OnNewTaskClicked"
                    HorizontalOptions="Start"/>
            
            <SearchBar Placeholder="Rechercher..."
                      WidthRequest="200"
                      HorizontalOptions="End"/>
        </HorizontalStackLayout>

        <!-- Liste des tâches -->
        <CollectionView Grid.Row="1"
                      ItemsSource="{Binding Tasks}"
                      SelectionMode="Single"
                      SelectionChanged="OnTaskSelected">
            
            <CollectionView.EmptyView>
                <VerticalStackLayout HorizontalOptions="Center" 
                                   VerticalOptions="Center"
                                   Spacing="10">
                    <Label Text="Aucune tâche trouvée"
                           FontSize="18"
                           FontAttributes="Bold"
                           HorizontalOptions="Center"/>
                    <Label Text="Cliquez sur 'Nouvelle Tâche' pour en créer une"
                           FontSize="14"
                           HorizontalOptions="Center"/>
                </VerticalStackLayout>
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="10" Padding="10">
                        <Grid ColumnDefinitions="*, Auto, Auto, Auto">
                            <VerticalStackLayout>
                                <Label Text="{Binding Titre}"
                                       FontSize="16"
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding Description}"
                                       FontSize="14"/>
                                
                                <Grid ColumnDefinitions="Auto,Auto,Auto,Auto" ColumnSpacing="15">
                                    <!-- Échéance -->
                                    <Label Grid.Column="0">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Échéance: " FontAttributes="Bold"/>
                                                <Span Text="{Binding Echeance, StringFormat='{0:dd/MM/yyyy}'}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    
                                    <!-- Catégorie -->
                                    <Label Grid.Column="1">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Catégorie: " FontAttributes="Bold"/>
                                                <Span Text="{Binding Categorie}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>

                                    <!-- Statut -->
                                    <Label Grid.Column="2">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Statut: " FontAttributes="Bold"/>
                                                <Span Text="{Binding Statut}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    
                                    <!-- Priorité -->
                                    <Label Grid.Column="3">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="Priorité: " FontAttributes="Bold"/>
                                                <Span Text="{Binding Priorite}"/>
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                </Grid>
                            </VerticalStackLayout>
                            
                            <HorizontalStackLayout Grid.Column="1" Spacing="5">
                                <Button Text="Détails"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:TasksViewModel}}, Path=ViewDetailsCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="LightGreen"/>
                                <Button Text="Modifier"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:TasksViewModel}}, Path=ModifyTaskCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="LightBlue"/>
                                <Button Text="Supprimer"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewModels:TasksViewModel}}, Path=DeleteTaskCommand}"
                                        CommandParameter="{Binding .}"
                                        BackgroundColor="LightCoral"/>
                            </HorizontalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage> 